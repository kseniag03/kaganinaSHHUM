//
//  Tag.swift
//  kaganinaSHHUM
//

import Foundation
/*
final class Subscribtion {
    let id: Int
    let userId: Int
    let subscriberId: String
    
    init(id: Int, userId: Int, subscriberId: String) {
        self.id = id
        self.userId = userId
        self.subscriberId = subscriberId
    }
}

final class User {
    let id: Int
    let name: String
    let bio: String?
    let email: String
    let userPicture: String // ???
    let isAdmin: Bool
    let password: String
    
    init(id: Int, name: String, bio: String?, email: String, userPicture: String, isAdmin: Bool, password: String) {
        self.id = id
        self.name = name
        self.bio = bio
        self.email = email
        self.userPicture = userPicture
        self.isAdmin = isAdmin
        self.password = password
    }
}

final class SoundCard {
    let id: Int
    let name: String
    let description: String?
    let audio: String // ???
    let image: String // ???
    let location: String
    let dateCreated: Date
    let author: Int
    let userId: Int
    let recorder: String
    
    init(id: Int, name: String, description: String?, audio: String, image: String, location: String, dateCreated: Date, author: Int, userId: Int, recorder: String) {
        self.id = id
        self.name = name
        self.description = description
        self.audio = audio
        self.image = image
        self.location = location
        self.dateCreated = dateCreated
        self.author = author
        self.userId = userId
        self.recorder = recorder
    }
}

final class LikeSoundCard {
    let id: Int
    let userId: Int
    let soundCardId: Int
    
    init(id: Int, userId: Int, soundCardId: Int) {
        self.id = id
        self.userId = userId
        self.soundCardId = soundCardId
    }
}

final class SoundCardTag {
    let id: Int
    let userId: Int
    let soundCardId: Int
    
    init(id: Int, userId: Int, soundCardId: Int) {
        self.id = id
        self.userId = userId
        self.soundCardId = soundCardId
    }
}

final class Tag {
    let id: Int
    let title: String
    
    init(id: Int, title: String) {
        self.id = id
        self.title = title
    }
}

final class LikeTag {
    let id: Int
    let userId: Int
    let tagId: Int
    
    init(id: Int, userId: Int, tagId: Int) {
        self.id = id
        self.userId = userId
        self.tagId = tagId
    }
}
*/
